# 大容量存储设备支持

大容量存储支持可将飞控的 MicroSD 卡或 Flash 芯片挂载为电脑上的外置存储设备，便于直接拖放下载日志文件。对于板载 Flash，这种方式通常比通过 Configurator 下载日志更快。

目前仅 F4/G4/F7/H7 板卡支持该功能。

在 CLI 中输入 `msc` 并回车，飞控会重启进入大容量存储模式。进入后飞控会停止正常运行，直到断电再上电重启。
