---
sidebar_position: 11
---

# 舵机选项卡

![舵机页面](/img/betaflight_configurator_servos_tab.png)

可配置舵机响应某一个接收机通道。使用 `MIN`、`MID` 和 `MAX` 设置舵机行程端点，然后勾选用于控制该舵机的通道。

`Direction and Rate` 是一个权重设置，用于控制舵机的运动方向和行程范围。

启用实时模式后，地面站运行期间会实时驱动舵机。
